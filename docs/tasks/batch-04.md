# BATCH-04 OA 与电话通知框架

- `task_id`：BATCH-04
- 目标：完成 OA 发起、状态回写、事件投影和失败隔离框架；未确认的真实平台与电话模式保持关闭。
- 规则：RULE-002、004、005、014、016、017；受 Q-001～Q-005 阻塞。
- API/事件：内部 OA 状态回调、登记 OA 状态回写、`OA_APPROVAL_UPDATED`。
- 数据：扩展 `oa_process`；OA 调用与状态更新时间；登记 Outbox 复用。
- 不做：虚构真实 OA 协议、人工/自动电话任务、电话结果状态。
- 门禁：无 `OaAdapter` 时 OA 服务启动失败；电话模式为 `UNDECIDED` 时勾选提交被拒绝。

